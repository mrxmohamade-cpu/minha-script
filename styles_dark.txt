/* Calm, RTL-friendly dark theme with a single accent and subtle warnings */

QWidget {
    background-color: #0e1420;
    color: #e7ecf5;
    font-family: "Noto Sans Arabic", "Cairo", "Tajawal", "Segoe UI", sans-serif;
    font-size: 10.5pt;
    font-weight: 500;
    letter-spacing: 0.05px;
}

QMainWindow {
    background-color: #0e1420;
}

/* Header and subscription row */
QFrame#HeaderFrame {
    background: #10192a;
    border: 1px solid rgba(255, 255, 255, 0.04);
    border-radius: 10px;
    padding: 8px 12px;
}

QLabel#HeaderTitle {
    color: #f8fafc;
    font-size: 15pt;
    font-weight: 800;
}

QLabel#HeaderSubtitle {
    color: #b5c3d6;
    font-size: 9.3pt;
    font-weight: 500;
}

QLabel#SubscriptionBadge {
    background: rgba(59, 130, 246, 0.15);
    color: #dbeafe;
    padding: 6px 12px;
    border: 1px solid rgba(59, 130, 246, 0.35);
    border-radius: 12px;
    font-weight: 700;
    min-width: 150px;
    qproperty-alignment: 'AlignCenter';
}

QPushButton#SubscriptionManageButton,
QPushButton#SubscriptionRefreshButton {
    background: #0f172a;
    color: #e7ecf5;
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 9px;
    padding: 7px 12px;
    font-weight: 700;
}
QPushButton#SubscriptionManageButton:hover,
QPushButton#SubscriptionRefreshButton:hover {
    border-color: #3b82f6;
    color: #ffffff;
}

/* Filter/search bar */
QFrame#FilterBar {
    background: #101a2b;
    border: 1px solid rgba(255, 255, 255, 0.04);
    border-radius: 10px;
    padding: 4px 8px;
}

QLineEdit, QComboBox {
    background: #0f1829;
    color: #e7ecf5;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 8px;
    padding: 7px 10px;
}
QLineEdit:focus, QComboBox:focus {
    border: 1px solid #3b82f6;
    box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.22);
}
QComboBox QAbstractItemView {
    background: #0f1829;
    selection-background-color: rgba(59, 130, 246, 0.18);
    selection-color: #e7ecf5;
}

/* Main table focus */
QFrame#ContentFrame {
    background: #0f1625;
    border: 1px solid rgba(255, 255, 255, 0.02);
    border-radius: 10px;
}

QTableWidget {
    gridline-color: rgba(255, 255, 255, 0.06);
    selection-background-color: rgba(59, 130, 246, 0.22);
    selection-color: #f8fafc;
    alternate-background-color: rgba(255, 255, 255, 0.02);
}
QHeaderView::section {
    background-color: #101a2b;
    color: #e7ecf5;
    padding: 6px 8px;
    border: 1px solid rgba(255, 255, 255, 0.04);
    font-weight: 700;
}
QTableCornerButton::section {
    background-color: #101a2b;
    border: 1px solid rgba(255, 255, 255, 0.04);
}
QTableWidget QTableWidgetItem {
    padding: 4px;
}

/* Buttons row */
QPushButton#add_member_button,
QPushButton#remove_member_button,
QPushButton#start_button,
QPushButton#stop_button,
QPushButton#check_single_button,
QPushButton#download_pdfs_button,
QPushButton#open_messages_button,
QPushButton#toggle_search_filter_action,
QPushButton#export_button,
QPushButton#import_button {
    background: #142036;
    color: #e7ecf5;
    border: 1px solid rgba(255, 255, 255, 0.07);
    border-radius: 9px;
    padding: 8px 14px;
    font-weight: 700;
}
QPushButton#start_button { background: rgba(59, 130, 246, 0.15); border-color: rgba(59, 130, 246, 0.45); }
QPushButton#stop_button { background: rgba(245, 158, 11, 0.12); border-color: rgba(245, 158, 11, 0.45); }
QPushButton:hover {
    border-color: #3b82f6;
}
QPushButton:disabled {
    background: #0f172a;
    color: #6b7280;
    border-color: rgba(255, 255, 255, 0.04);
}

/* Status bar */
QStatusBar#MainStatusBar {
    background: #0f172a;
    color: #dbeafe;
    border-top: 1px solid rgba(255, 255, 255, 0.06);
    padding: 4px 6px;
}
QLabel#StatusDetail { color: #dbeafe; font-weight: 700; }
QLabel#CountdownLabel { color: #9fb3d6; }
QLabel#LastEventLabel { color: #9fb3d6; }

/* Table status text color cues */
QTableWidget::item[selected] {
    background-color: rgba(59, 130, 246, 0.22);
    color: #f8fafc;
}

/* Softer scrollbars */
QScrollBar:vertical, QScrollBar:horizontal {
    background: #0f172a;
    width: 12px;
    height: 12px;
    margin: 0px;
    border-radius: 6px;
}
QScrollBar::handle {
    background: rgba(255, 255, 255, 0.15);
    border-radius: 6px;
}
QScrollBar::handle:hover {
    background: rgba(59, 130, 246, 0.4);
}
QScrollBar::add-line, QScrollBar::sub-line {
    background: transparent;
    height: 0px;
    width: 0px;
}

/* Toasts/dialog hints */
QDialog {
    background: #0f1625;
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 10px;
}

QToolTip {
    background: #111a2a;
    color: #e7ecf5;
    border: 1px solid rgba(59, 130, 246, 0.35);
    border-radius: 6px;
    padding: 6px;
}

/* Highlight warning tone */
.warning {
    color: #fbbf24;
}
